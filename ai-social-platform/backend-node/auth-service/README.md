# Auth Service for AI Social Platform

This service implements authentication features: Google OAuth (ID token verification), mobile OTP, JWT access tokens, refresh tokens, email verification, password reset, device/session management, rate limiting, and RBAC.

Environment variables: see .env.example

Run in development:
- npm install
- npm run dev

